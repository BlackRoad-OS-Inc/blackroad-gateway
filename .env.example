# BlackRoad Gateway — Environment Variables
# Copy to .env and fill in values. NEVER commit .env

# ── Gateway binding ───────────────────────────────────────────────────────────
BLACKROAD_GATEWAY_BIND=127.0.0.1
BLACKROAD_GATEWAY_PORT=8787

# ── AI Provider keys — ONLY set in gateway environment, NEVER in agents ───────
# Anthropic Claude
BLACKROAD_ANTHROPIC_API_KEY=sk-ant-...

# OpenAI / compatible
BLACKROAD_OPENAI_API_KEY=sk-...
BLACKROAD_OPENAI_BASE_URL=https://api.openai.com/v1

# Ollama (local LLM — no key required)
BLACKROAD_OLLAMA_URL=http://127.0.0.1:11434

# ── Rate limiting ─────────────────────────────────────────────────────────────
GATEWAY_RATE_LIMIT_MAX=100
GATEWAY_RATE_LIMIT_WINDOW_MS=60000

# ── Audit log ─────────────────────────────────────────────────────────────────
AUDIT_LOG=/var/blackroad/audit/gateway-audit.jsonl

# ── Default provider priority ─────────────────────────────────────────────────
# Comma-separated: first available wins
GATEWAY_PROVIDER_ORDER=ollama,anthropic,openai

NODE_ENV=production
